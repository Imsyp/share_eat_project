<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시물 상세 보기</title>
    <link href="/css/style.css" rel="stylesheet">
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <!-- google font link-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Hi+Melody&display=swap" rel="stylesheet">
    <style>
        .font{
            font-family: "Hi Melody", sans-serif;
        }
        #post {
            display: flex;
            flex-direction: column;
            width: 90%;
            height: auto;
            max-width: 1000px;
            background: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
            overflow: hidden;
            margin: 0 auto; 
            padding: 20px; /* 내부 여백 추가 */
            box-sizing: border-box; /* 내부 여백을 포함한 크기 지정 */
        }
        /* 추가적인 스타일링 */
        #post h2 {
            font-size: 24px; /* 헤더의 텍스트 크기 조정 */
            margin-bottom: 15px; /* 헤더 아래 간격 추가 */
        }
        .button-container {
            display: flex;
            gap: 10px; /* 두 버튼 사이의 간격을 설정합니다 */
        }
        .chat, .cancel_button {
            width: 100%;
            max-width: 100px;
            padding: 10px;
            border: none;
            border-radius: 5px;
            color: #fff;
            font-size: 18px;
            cursor: pointer;
            text-align: center;
            white-space: nowrap;
        }
        .chat {
            background-color: #929292;
        }
        .cancel_button {
            background-color: #bbbbbb;
        }
        .chat:hover {
            background-color: #4e4e4e; 
        }
        .cancel_button:hover {
            transition: 0.5s;
            background-color: #929292;
        }
        #post h2 {
            font-size: 24px; /* 헤더의 텍스트 크기 조정 */
            margin-bottom: 15px; /* 헤더 아래 간격 추가 */
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .cancel-button {
            width: 80%;
            max-width: 50px;
            padding: 10px;
            border: none;
            border-radius: 5px;
            color: #3e3e3e;
            font-size: 15px;
            cursor: pointer;
            text-align: center;
            white-space: nowrap;
        }
        .cancel-button {
            background-color: #e8e8e8;
        }
        .cancel-button:hover {
            transition: 0.5s;
            background-color: #929292;
        }
    </style>
</head>
<body class="grey-bg">
    <%- include('sidebar') %>
    <div id="content">
        <div class="navbar">
            <a class="unselected" href="/user/flash_purchase">번개공구</a>
            <a class="selected" href="/user/regular_purchase">정기공구</a>
        </div>
        <hr>
        <div id="post">
            <div class="post-box">
                <br><br>
                <img src="<%= post.img %>" style="width: 500px; height: auto;">
                <h2>
                    <span>
                        <img src="<%=userprofile.profile%>" style="width: 38px; height: 38px; border-radius: 50%;"><%= post.username %>님
                    </span>
                    <span>
                        <% if (currentUser.toString() === post.user.toString()) { %>
                            <a href="/user/edit_flash/<%=post._id%>" class="cancel-button">수정하기</a>
                            <a href="/user/delete_flash/<%=post._id%>"class="cancel-button">삭제하기</a>
                        <% } %>
                    </span>
                </h2><br>
                <p> 제품명: <%= post.product_name %></p>
                <p> 수량: <%= post.quantity %></p>
                <p> 모집인원: <%= post.number_of_recruits %></p>
                <p> 총액: <%= post.total_amount %></p>
                <p> 거래 장소: <%= post.location %></p>
                <p> 게시글 업로드 날짜 및 시간: <%= post.date %></p>
                <p> 게시글 업로드 날짜 및 시간: <%= post.date %></p>
                <p> 공동구매 희망 시간: <%= post.datey%> <%= post.time%></p>
            </div>
            <br>
            <div class="button-container">
                <a href="/user/chat/request?writerId=<%= post.username%>" class="chat">채팅하기</a>
                <a href="/user/regular_purchase" class="cancel_button">취소</a>
            </div>
        </div>    
    </div>
</body>
</html>
