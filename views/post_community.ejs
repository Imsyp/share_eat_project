<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>게시물 상세 보기</title>
    <link href="/css/style.css" rel="stylesheet">
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <!-- google font link-->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Hi+Melody&display=swap" rel="stylesheet">
    <style>
        .font{
            font-family: "Hi Melody", sans-serif;
        }
        /* 게시물 박스 스타일 */
        .post-box {
            padding: 20px;
            margin-bottom: 20px;
            border: 1px solid #ddd; /* 본문과 댓글 공간 구분 */
            border-radius: 10px;
            background-color: #fff;
        }
        /* 게시물 제목 스타일 */
        .post-box h2 {
            font-size: 30px;
            margin-bottom: 15px;
            border-bottom: 2px solid #eee;
            padding-bottom: 10px;
        }
        /* 게시물 내용 스타일 */
        .post-box p {
            font-size: 20px;
            line-height: 1.6;
        }
        /* 게시물 이미지 스타일 */
        .post-box img {
            max-width: 100%;
            height: auto;
            margin-top: 15px;
            border-radius: 10px;
        }

        .write-form input[name="content"] {
            width: 60%;
            height: 30px; 
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 16px;
            margin-bottom: 10px;
            box-sizing: border-box;
        }
        .comment-box{
            margin-left: 20px;
            padding: 20px;
            border: 1px solid #ddd; /* 본문과 댓글 공간 구분 */
            border-radius: 10px;
            background-color: #f9f9f9;
        }
        .box{
            padding: 10px;
            border: 1px solid #bebebe;
            border-radius: 10px;
        }
        .date {
            font-size: 12px;
            color: #999;
            text-align: right; /* 오른쪽 정렬 */
        }
        .write-form{
            margin-left: 20px;
        }
    </style>
</head>
<body class="grey-bg">
    <div id="sidebar">
        <div>
            <a href="/"><img src="/images/logo_non_bg.png" alt="Logo" width="250px"></a>
        </div>
        <p><span class="accent font">쉐어 잇</span></p>
        <p><span class="share font">share eat,<br>share it!</span></p>
        <a href="/user/mypage">마이 페이지</a>
        <a href="/user/logout">로그아웃</a>
        <a href="/user/community_board">게시판</a>
        <a href="/user/flash_purchase">공동구매</a>
        <a href="/user/chat/list">채팅</a>
    </div>
    <div id="content">
        <div class="navbar">
            <a class="selected" href="/user/community_board">자유게시판</a>
            <a class="unselected" href="/user/information_board">정보게시판</a>
        </div>
        <hr>
        <div class="post-box">
            <h2><%= post.title %></h2>
            <div style="display: flex; align-items: center; justify-content: space-between;">
                <img src="<%=user.profile%>" style="width:50px; height: 50px; border-radius: 50%; margin-right: 10px;">
                <div style="display: flex; flex-direction: column;">
                    <p style="margin: 0; font-weight: bold;"><%=user.username%></p>
                    <p style="margin: 0; font-size: 12px; color: #999;"><%=post.date%></p>
                </div>
                <% if (currentUser.toString() === post.user.toString()) { %>
                    <div>
                        <a href="/user/edit_community/<%=post._id%>" style="margin-left: auto; padding: 5px 10px; text-decoration: none; background-color: #007bff; color: white; border-radius: 5px;">수정하기</a>
                        <a href="/user/delete_community/<%=post._id%>" style="margin-left: auto; padding: 5px 10px; text-decoration: none; background-color: #007bff; color: white; border-radius: 5px;">삭제하기</a>
                    </div>
                <% } %>
                <a href="/user/chat/request?writerId=<%= post.username%>" style="margin-left: auto; padding: 5px 10px; text-decoration: none; background-color: #007bff; color: white; border-radius: 5px;">채팅하기</a>
            </div>
            
            <hr>
            <p><%- post.content %></p>
            <img src="<%= post.img %>">
            
        </div>
        <form action="/user/comment_community" method="post" class="write-form">
            <h1>댓글</h1>
            <input name="content" placeholder="댓글을 입력해주세요">
            <input name="parentId" value="<%= post._id %>" style="display: none;">
            <button type="submit">전송</button>
            <hr>
        </form>
        <div class="comment-box">
            <% if (comment && comment.length > 0) { %>
                <% for (let i = 0; i < comment.length; i++) { %>
                    <div class ="box">
                        <p>작성자: <%= comment[comment.length -1 -i].username %></p>
                        <hr>
                        <p><%= comment[comment.length -1 -i].content %></p>
                        <p class="date"><%= comment[comment.length -1 -i].date %></p>
                    </div>
                    <br><br>
                <% } %>
            <% } else { %>
                <p>댓글이 존재하지 않습니다</p>
            <% } %>
        </div>
    </div>
</body>
</html>
